"""非推論条件のプロンプト言い換えバリエーション（v2〜v5）。

v1 は各 008_* スクリプトで使用した既存プロンプト（reranker.PROMPT_CONFIGS 参照）。
バリエーション番号ごとに文体テンプレートを条件間で共有する:
- v2: 「以下に〜を示します」調
- v3: 「〜すること」調
- v4: 自然文調
- v5: 「タスク:」調
例示ブロックとユーザプロンプトは全バリエーションで v1 と共通。
表記は v1 のみ「モウラ」（原文ママ）、v2 以降は「モーラ」。
"""

PROMPT_VARIATIONS: dict[str, dict[str, str]] = {
    "simple": {
        "v2": """
以下に Query と Wordlist を示します。Wordlist の単語を、Query と発音が近い順に並べ替えてください。
結果は上位 Top N 件のインデックスだけを出力してください。
""",
        "v3": """
Query（クエリ）と Wordlist（単語リスト）が入力されます。単語リストを、クエリとの発音の類似度が高い順にソートしてください。
上位 Top N 件のインデックスのみを返すこと。
""",
        "v4": """
与えられた Wordlist の単語を、Query との発音の近さの順にランキングしてください。
出力には上位 Top N 件のインデックスのみを含めてください。
""",
        "v5": """
タスク: 音韻類似度によるリランキング。入力は Query と Wordlist です。
Query と発音が似ている順に Wordlist を並べ替え、上位 Top N 件のインデックスのみを出力してください。
""",
    },
    "detailed": {
        "v2": """
以下に Query と Wordlist を示します。Wordlist の単語を、Query と発音が近い順に並べ替えてください。
並べ替えでは、子音の一致よりも母音の一致を重視してください。
また、モーラ数が Query と一致する単語を優先してください。なお、促音（ッ）・撥音（ン）・長音（ーなど長音化に相当する表記）が挿入・削除されていても構いません。
結果は上位 Top N 件のインデックスだけを出力してください。
""",
        "v3": """
Query（クエリ）と Wordlist（単語リスト）が入力されます。単語リストを、クエリとの発音の類似度が高い順にソートしてください。
判断の際は、母音の一致を子音の一致より優先すること。
クエリとモーラ数が等しい単語を高く評価すること。ただし、促音（ッ）、撥音（ン）、長音（ーや長音化に相当する表記）の有無の違いは許容すること。
上位 Top N 件のインデックスのみを返すこと。
""",
        "v4": """
与えられた Wordlist の単語を、Query との発音の近さの順にランキングしてください。
母音が一致しているかどうかを最も重視し、子音の一致はその次としてください。
モーラ数が Query と同じ単語を優先してください（促音「ッ」、撥音「ン」、長音「ー」などの挿入・削除は近さを損なわないものとみなします）。
出力には上位 Top N 件のインデックスのみを含めてください。
""",
        "v5": """
タスク: 音韻類似度によるリランキング。入力は Query と Wordlist です。
Query と発音が似ている順に Wordlist を並べ替えてください。その際、(a) 子音より母音の一致を優先する、(b) Query とモーラ数が同じ単語を優先する（促音（ッ）・撥音（ン）・長音（ー等）の挿入や削除は許容）、という基準に従ってください。
上位 Top N 件のインデックスのみを出力してください。
""",
    },
    "step_by_step": {
        "v2": """
以下に Query と Wordlist を示します。Wordlist の単語を、Query と発音が近い順に並べ替えてください。
次の手順に従ってください。
(1) Query と各単語から促音（ッ）・撥音（ン）・長音（ー）を取り除く。
(2) それぞれを小文字のローマ字表記に変換する。
(3) 連続する同じ母音や ei、ou は長音相当とみなして1つにまとめる。
(4) まず母音列の一致度で比較し、母音の一致度が同程度の場合は子音の類似度で比較する。
結果は上位 Top N 件のインデックスだけを出力してください。
""",
        "v3": """
Query（クエリ）と Wordlist（単語リスト）が入力されます。単語リストを、クエリとの発音の類似度が高い順にソートしてください。
判断は次のステップで行うこと。
ステップ1: クエリと比較対象の単語から、促音（ッ）、撥音（ン）、長音（ー）を削除する。
ステップ2: 両者を小文字ローマ字に変換する。
ステップ3: 同一母音の連続および ei、ou を長音相当として縮約する。
ステップ4: 母音列の一致を最優先し、同程度なら子音の類似性で順位付けする。
上位 Top N 件のインデックスのみを返すこと。
""",
        "v4": """
与えられた Wordlist の単語を、Query との発音の近さの順にランキングしてください。
まず、Query と各単語から促音（ッ）・撥音（ン）・長音（ー）を取り除いてください。次に、それぞれを小文字のローマ字に直してください。そのうえで、同じ母音の連続や ei、ou は長音に相当するものとして1つにまとめてください。最後に、母音列がどれだけ一致するかを優先して比較し、母音の一致が同程度の単語どうしは子音の類似性で比較してください。
出力には上位 Top N 件のインデックスのみを含めてください。
""",
        "v5": """
タスク: 音韻類似度によるリランキング。入力は Query と Wordlist です。
次の手続きで Query と発音が似ている順に Wordlist を並べ替えてください。
(a) 促音（ッ）・撥音（ン）・長音（ー）を Query と各単語から削除する。
(b) 小文字ローマ字へ変換する。
(c) 同じ母音の連続や ei、ou は長音相当として縮約する。
(d) 母音列の一致を優先し、同程度の場合のみ子音の類似性を比較する。
上位 Top N 件のインデックスのみを出力してください。
""",
    },
}
